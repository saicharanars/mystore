
services:
  # nx-app:
  #   build:
  #     context: .
  #   environment:
  #     NODE_ENV: development
  #   ports:
  #     - '3000:3000'
  #     - '3001:3001'
  #   volumes:
  #     - .:/usr/src/app  # Mount the current directory to /usr/src/app in the container
  #     - /usr/src/app/node_modules  # Prevent overwriting node_modules with empty host directory
  #   command: pnpm dev  # Use a dev command to enable live reloading
  #   depends_on:
  #     - postgresdb  # Ensure the database is started before the app

  postgresdb:
    image: postgres:14-bookworm # Use the latest version of Postgres
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydatabase
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data # Persist database data

volumes:
  pgdata: # Define a named volume for persistent storage
  # node_modules:  # Define a named volume for node_modules to avoid host-container conflicts
